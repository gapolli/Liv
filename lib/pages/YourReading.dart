import 'package:flutter/material.dart';

class YourReading extends StatelessWidget {
  YourReading({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('Your Reading'),
    );
  }
}
